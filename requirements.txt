# Core inference (GPU required for phases 9–13)
vllm>=0.6.0
transformers>=4.45.0
torch>=2.3.0

# Embeddings / semantic evaluation
sentence-transformers>=3.0.0

# Data / parsing
numpy>=1.26.0
tqdm>=4.66.0
lxml>=5.0.0          # phase12: CURD XML ground-truth parsing

# Dev
pytest>=7.4.0
